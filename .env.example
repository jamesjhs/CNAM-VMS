# Database
DATABASE_URL="postgresql://user:password@localhost:5432/cnam_vms"

# Auth.js / NextAuth
AUTH_SECRET="generate-with-openssl-rand-base64-32"
# AUTH_URL is optional when behind a reverse proxy that forwards the correct
# Host and x-forwarded-proto headers — Auth.js will auto-detect the origin.
# Set it explicitly only if redirects land on the wrong host, e.g.:
# AUTH_URL="https://cnamvms.example.com"

# SMTP — used for 2FA one-time password (OTP) emails sent during sign-in
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="noreply@example.com"
EMAIL_SERVER_PASSWORD="your-smtp-password"
EMAIL_FROM="CNAM VMS <noreply@example.com>"

# Bootstrap / Seeding
ROOT_USER_EMAIL="admin@example.com"
ROOT_USER_NAME="Root Admin"

# File Uploads
UPLOAD_DIR="/var/uploads/cnam-vms"
UPLOAD_MAX_SIZE_MB="10"
